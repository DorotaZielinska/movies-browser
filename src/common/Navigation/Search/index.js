import { Wrapper, Input } from "./styled";

export const Search = () => {

    return (
        <>
            <Wrapper>
                <Input placeholder="Search for movies..." />
            </Wrapper>
        </>
    );
};