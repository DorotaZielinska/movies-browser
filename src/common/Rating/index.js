import { RatingContainter, RatingIcon, Vote, Votes } from "./styled";

export const Rating = () => (
    <RatingContainter>
        <RatingIcon />
        <Vote></Vote>
        <Votes></Votes>
    </RatingContainter>
);